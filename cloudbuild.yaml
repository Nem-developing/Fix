logsBucket: 'gs://prod-nehemiebarkia-fix-builds'

options:
  logging: GCS_ONLY

steps:
  - name: 'hello-world'
    entrypoint: 'bash'
    args:
      - '-c'
      - 'echo "hello world, tag : $TAG_NAME"'
